services:
  db:
    image: postgres:16-alpine
    ports:
      - 5434:5432
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=meuapp
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: